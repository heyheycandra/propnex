import 'package:flutter/material.dart';

const sampleAppOutterMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const sampleAppMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const sampleAppMarginHorizontal = EdgeInsets.only(left: 17, right: 17);

const sampleAppMarginBtn = EdgeInsets.only(top: 12, bottom: 12);

const sampleAppDashboardPadding = EdgeInsets.only(top: 16, bottom: 16);

const sampleAppOutterPadding = EdgeInsets.only(top: 16, right: 24, left: 24);

const sampleAppInnerPadding = EdgeInsets.only(top: 16, right: 12, left: 12, bottom: 16);

const sampleAppSearchFieldPadding = EdgeInsets.only(top: 16, right: 17, left: 17);

const sampleAppParagraph = EdgeInsets.only(left: 8);

const sampleAppBoxedParagraph = EdgeInsets.only(left: 8, top: 16, bottom: 16);

const sampleAppDialogPadding = EdgeInsets.only(top: 30, right: 24, left: 24, bottom: 20);
